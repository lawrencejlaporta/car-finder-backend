services:
  - type: web
    name: car-finder-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
```

5. **Commit the changes**

---

### Step 2: Also Update in Render Settings

Let's manually fix the build command in Render:

1. **In Render dashboard**, click **"Settings"** (left sidebar)
2. Scroll to **"Build & Deploy"** section
3. Change **Build Command** to: 
```
   npm install
```
4. Change **Start Command** to:
```
   node server.js
